name: tests

on:
  push:
  pull_request:

jobs:
  lua:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
      - uses: leafo/gh-actions-lua@v13
        with:
          luaVersion: "5.4"
      - run: lua tests/run.lua
